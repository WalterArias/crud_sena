/* let ejemplo = process.env; //api node process ::: todas las varibales de configuracion
console.log(ejemplo); */

const path = require("path");

// variables de configuracion detalladas
/* console.log(
  process.env.USERNAME,
  process.env.OS,
  process.env.NUMBER_OF_PROCESSORS
);
 */
console.log(path.delimiter);
console.log(path.basename);
